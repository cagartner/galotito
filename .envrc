# Personal GitHub context for this repo (loaded by direnv).
# Points the gh CLI at a separate config dir holding the personal `cagartner` login,
# so `gh pr`, `gh issue`, etc. use the personal account here instead of the global (work) one.
export GH_CONFIG_DIR="$HOME/.config/gh-personal"
